
public class FollowCamera : MonoBehaviour
{
    // 카메라가 따라다닐 대상을 가리키는 변수
    [SerializeField] GameObject thingToFollow;

    void LateUpdate()
    {
        transform.position = thingToFollow.transform.position + new Vector3(0, 0, -30);
       
    }
}
